import React, { useState } from "react";
import {
  Box,
  TextField,
  IconButton,
  Button,
  Slider,
  useTheme,
} from "@mui/material";

const UmapConfig: React.FC = () => {
  return <Box>Hello! UmapConfig</Box>;
};

export default UmapConfig;
